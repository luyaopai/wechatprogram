<!--pages/home/home.wxml-->
<view class="box">
  <view class="head">
    <category list="{{newsTypes}}" bind:chooseList="chooseList" bind:tap="handClick"></category>
  </view>
  <view class="home_list">
    <list detail="{{detail}}" bind:checkDetail="checkDetail"></list>
  </view>
</view>